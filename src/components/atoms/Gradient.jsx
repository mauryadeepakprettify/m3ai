const Gradient = ({ className }) => {
  return (
    <span
      className={`absolute -z-10 h-[351px] w-[351px] -translate-x-1/2 -translate-y-1/2 rounded-full ${className}`}
    ></span>
  );
};

export default Gradient;
